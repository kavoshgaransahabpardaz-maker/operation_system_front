services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    environment:
      - BACKEND_URL=https://api.veritariffai.co
    restart: unless-stopped
    networks:
      - brokerai-web

networks:
  brokerai-web:
    external: true
